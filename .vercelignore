# Exclude web app (only deploying server)
app/web

# Exclude docs
md/

# Exclude server source (keep dist)
app/server/src

# Exclude dev files
*.log
.env
.env.*
app/server/.env
app/server/.env.*

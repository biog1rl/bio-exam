{
	"version": 2,
	"installCommand": "corepack enable && yarn install",
	"buildCommand": "node app/server/vercel-build.cjs",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api"
		}
	]
}

{
	"version": 2,
	"installCommand": "corepack enable && cd ../.. && yarn install",
	"buildCommand": "node vercel-build.cjs",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api"
		}
	]
}
